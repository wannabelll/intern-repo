pipeline {
    agent {
        label "default-1"  // The label corresponds to your worker node
    }    
    stages {
       
        stage('Last stage') {
            steps {
                // Example build command, adjust as needed
                echo 'ok'
            }
        }
    }
}

